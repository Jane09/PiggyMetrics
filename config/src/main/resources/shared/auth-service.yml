spring:
  data:
    mongodb:
      host: auth-mongodb
      username: entity
      password: ${MONGODB_PASSWORD}
      database: piggymetrics
      port: 27017

server:
  servlet:
    context-path: /uaa
  port: 5000
